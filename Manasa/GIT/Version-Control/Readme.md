1.Create a folder named git-assignment and initialize Git repository
2.Create file notes.txt and add:
This is version 1
→ Commit it
3.Add new line in same file:
This is version 2
→ Commit it
4. Add new line again:
This is version 3
→ Commit it
5.Check commit history using:
git log --oneline
6.Rollback to Version 1 temporarily using checkout command and verify file content

